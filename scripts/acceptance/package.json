{
  "name": "cogno-chain-acceptance",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Acceptance smoke test against a running node: the runtime's call surface is sudo-free, observer-written, anchoring-dropped and append-only; reads are node-served; and the CheckCapacity gate rejects an unfunded post at the pool.",
  "scripts": {
    "test": "node acceptance.mjs"
  },
  "dependencies": {
    "@polkadot/api": "^16.5.6"
  }
}
