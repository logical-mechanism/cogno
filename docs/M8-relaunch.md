# M8 — talk_vault contract relaunch (LIVE on preprod) + wallet UX

The L-01 audit fix (PR #4) rewrote `contracts/validators/talk_vault.ak` (pin
`reference_script == None` on the spend continuation). That moved the compiled blueprint
hash and therefore the `min_lock`-applied policy id / vault address, **orphaning the live
M7 vault**. M8 relaunches the contract under the new hash, re-proves the whole live loop,
and (frontend) adds a CIP-30 wallet + encrypted posting keystore + a full polish pass.

## 0. The hash move

| Layer | Old (M7) | New (M8) |
|---|---|---|
| Blueprint hash (`plutus.json`) | `cf0712dc…26c6ba62` | `49ffbfc6…6237aeac` |
| Applied policy id / vault hash (min_lock=100 ADA) | `a82d0ad6…446cacf9` | `168a9710…52489c7` |
| Vault address (owner skh `10133fde…`) | `addr_test1zz5z6zkk…6t6ca8` | `addr_test1zqtg49cs…svljrte` |
| Beacon name (= app-chain identity hash) | `287a99d2…0ae6be75` | **unchanged** (pure fn of owner) |

Because the beacon **name** is `blake2b_256(cbor.serialise(owner))` — independent of the
script hash — the app-chain identity hash and the `CognoGate` bind (`287a99d2… → poster`)
**survive the relaunch**; only the vault address + beacon **policy id** move. No re-bind.

The applied artifact is regenerated by the new committed generator:
`node contracts/scripts/regen-vault.mjs` → `contracts/vault.json`
(`applyParamsToScript(compiledCode, [{int:100000000}], "JSON")` + `resolveScriptHash`,
the live `paramForm` and the exact path the in-browser lock uses). `aiken check` green
(533 checks, 38/38).

## 1. Live relaunch on preprod — DONE ✓ (2026-06-17)

Stack: synced preprod `cardano-node` v11.0.1 (Conway, tip slot ~126066xxx) + Ogmios :1337
+ Kupo :1442 + the `cogno-chain-node --dev :9944` (spec 107, genesis `2653e177…`) + the
Cogno-Follower :8090. Owner wallet `addr_test1qpsk23r…` (1331.9 → 1331.3 tADA after fees).

1. **Exit the orphaned vault** (`app/scripts/m2d-unlock.mjs`, against the OLD `vault.json`):
   spent `4c349dda…#0` (old policy `a82d0ad6`), burned beacon `287a99d2`, reclaimed
   99.67 tADA. **Exit tx `4cd28d234630c1f0275786aaf323bbcc804e9e0eaec8eed118356ffd264f2546`.**
2. **Regenerate** `contracts/vault.json` (vault hash `168a9710…`) and install it at
   `/tmp/cogno-m2/vault.json` (old backed up to `vault.old.json`).
3. **Re-lock** 100 tADA at the new vault (`m2d-lock.mjs`): minted beacon `287a99d2` under the
   new policy `168a9710`, inline `VaultDatum{owner}`, at `addr_test1zqtg49cs…svljrte`.
   **Lock tx `33202448b66e91389fc6ba68f555fdf02b673bb420cd143959fbf26fdcc87e30`** (slot 126066527).
4. **Kupo** re-indexed with the new policy match (`--match 168a9710….*`); the new vault UTxO
   `33202448…#0` (100 ADA + the beacon) observed.
5. **Committee `set_stake`** (`services/committee/sync-weight.mjs --via committee`): observed
   the new vault, looked up `AccountOf[287a99d2] → poster`, drove `talk_stake.set_stake(poster,
   100000000)` + `force_set_capacity` through the 3-of-5 FollowerCommittee (motion #10,
   Approved+Executed) — **zero sudo on the privileged path**.
6. **Feeless post** (`m2d-post.mjs`): `PostCreated id=2`, free-balance Δ = 0. Locked ADA →
   Cardano-sourced weight `100000000` → feeless post.

Final state (`services/committee/state.mjs`): `AccountOf[287a99d2] = poster`,
`AllowedStake = 100000000`, Microblog posts = 3 (M7 ids 0–1 + M8 id 2).

## 2. Tooling added

- `contracts/scripts/regen-vault.mjs` — the previously-missing committed generator for
  `vault.json`. Run it after any `aiken build` that changes the validator, then redeploy.
- `services/committee/state.mjs` — read-only chain-state inspector (genesis / spec / bind /
  weight / post count), the companion to `op.mjs` (writes) and `sync-weight.mjs`.

> Note (env): node here is the snap build whose stdout is `/dev/null`; run the off-chain
> scripts with the nvm node (`~/.nvm/versions/node/v22.12.0/bin/node`) to see their output.
> Importing `@meshsdk/core-cst` also redirects process stdio — the regen artifact (the
> written `vault.json`) is the canonical output; inspect with `jq`.
