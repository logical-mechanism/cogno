# Cogno-Follower (M2) — Python deps. The CIP-8 verify path is REUSED from cogno_v3 (do not
# reimplement): pycardano.cip.cip8.verify, pinned to the proven version. blake2b is stdlib
# (hashlib); the HTTP server is stdlib (http.server). pycardano pulls cose/cbor2 transitively.
#
# Easiest: reuse cogno_v3's venv (already has these). Or a fresh venv:
#   python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
pycardano==0.13.0
# M2d vault observation reads Cardano db-sync via psycopg 3 (Ogmios stays for L1 tx submission elsewhere):
psycopg[binary]==3.2.*
